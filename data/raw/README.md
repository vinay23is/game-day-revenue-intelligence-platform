# data/raw/

Place any externally downloaded raw datasets here (e.g., public Kaggle basketball datasets).
The project runs fully without external data — `src/simulate_business_data.py` generates all required CSVs in `data/simulated/`.
